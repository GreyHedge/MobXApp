import {ELanguages} from '../types';

export const MAX_ANIMATED_HEADER_HEIGHT = 70;
export const MIN_ANIMATED_HEADER_HEIGHT = 0;
export const COLOR_BUTTON_HEIGHT = 50;

export const languages: ELanguages[] = [ELanguages.EN, ELanguages.RU];
